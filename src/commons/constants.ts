/**
 * 1 second in milliseconds
 */
const SECOND_MS = 1000;

/**
 * 1 minute in seconds
 */
const MINUTE_S = 60;

const FILE_PREFIX = 'file://';
const BASE64_PREFIX = 'base64://';

export {
    SECOND_MS,
    MINUTE_S,
    FILE_PREFIX,
    BASE64_PREFIX
};
